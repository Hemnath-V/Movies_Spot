specVersion: "v1"
serviceId: "moviesspot"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "moviesspot-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
